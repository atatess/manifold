"""Manifold — local, type-aware diffusion code editing.

Phases 0-2 are a pure-Python library + CLI (no transport, no extension).
The denoising loop is runtime-agnostic; the forward pass is a swappable backend.
"""

__version__ = "0.0.1"
